pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo 'Pipeline is working!'
            }
        }

        stage('Done') {
            steps {
                echo 'All green 💚'
            }
        }
    }
}
