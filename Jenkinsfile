pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo "Repository checked out successfully"
            }
        }

        stage('Test Pipeline') {
            steps {
                echo "Pipeline is working!"
            }
        }
    }
}
